export let name="shrawan";
export let age=31;

let country="nepal";

export default country;

/* 
export by name 
prefix by export 
we can have many export by name in a file
export by default 
we have only one export by default in a file
 */